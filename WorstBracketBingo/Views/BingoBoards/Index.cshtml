@model IEnumerable<WorstBracketBingo.Models.BingoBoard>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.BracketID)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Title)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BracketID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BingoBoardID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BingoBoardID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BingoBoardID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
